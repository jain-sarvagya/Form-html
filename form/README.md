# HTML Form - Bio Data Collection

This is a basic HTML form project that collects personal and educational information from the user. It is styled using inline CSS and demonstrates the use of form elements such as inputs, radio buttons, select dropdowns, date pickers, and file uploads.

---

## 🚀 Features

- Collects personal details: first name, last name, gender, date of birth, phone number.
- Captures educational details: school name, class, stream, and marksheet upload.
- Uses `fieldset` and `legend` to group form sections for better readability.
- Submits data to a placeholder page: `thankyou.html`.

---

## 📂 Project Structure

/your-project-folder
│
├── index.html # Main HTML file with the form
├── thankyou.html # Redirect page after submission (to be created)
└── README.md # Project information

yaml
Copy code
